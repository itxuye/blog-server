type Query {
  user(id: ID): User
}

type Mutation {
  createUser(name: String!, email: String!, password: String!): User
}

type Subscription {
  newMessage: String
}

type User {
  id: ID
  name: String
  email: String
}
