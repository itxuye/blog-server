overwrite: true
schema: http://localhost:4000/graphql
require:
  - ts-node/register/transpile-only
  - tsconfig-paths/register
generates:
  ./src/typings/generated-models.d.ts:
    plugins:
      - typescript-common
      - typescript-server
      - typescript-mongodb